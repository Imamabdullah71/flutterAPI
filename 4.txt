Bagian PaymentPage, Ketika widget Switch dinyalakan akan membuka form field:
if (switchController.isSwitched.value) {
                  return TextFormField(
                    controller: daftarController,
                    decoration: InputDecoration(
                      labelText: 'Nama penghutang',
                      filled: true,
                      fillColor: Colors.white,
                      border: OutlineInputBorder(
                        borderRadius: BorderRadius.circular(8),
                      ),
                      focusedBorder: OutlineInputBorder(
                        borderRadius: BorderRadius.circular(8),
                        borderSide: const BorderSide(
                          color: Color.fromARGB(255, 114, 94, 225),
                        ),
                      ),
                      enabledBorder: OutlineInputBorder(
                        borderRadius: BorderRadius.circular(8),
                        borderSide: BorderSide(
                          color: Colors.grey.shade400,
                        ),
                      ),
                      contentPadding: const EdgeInsets.symmetric(
                        vertical: 16,
                        horizontal: 20,
                      ),
                    ),
                  );
                } else {
                  return Container();
                }
Dan mengganti fungsi tombol bayar menjadi masukkan ke daftar:
void masukkanKeDaftar() {
    // Fungsi
  }

Saya ingin memasukkan data hutang ke tabel hutang.
void masukkanKeDaftar() {
transaksiController.bayar.value =
        double.parse(uangDibayarController.text.replaceAll('.', ''));
    transaksiController.kembali.value =
        transaksiController.bayar.value - transaksiController.totalHarga.value;
    transaksiController.createTransaksiWithNoReceipt();
    transaksiController.createHutang();
  }

yang saya maksud createTransaksiWithNoReceipt() sama persis seperti fungsi 
createTransaksi() hanya saja tidak menjalankan fungsi generateReceipt dan tidak menjalankan fungsi upload_struk.

ketika Switch dinyalakan bayar.value itu harus lebih kecil dari totalHarga.value untuk membuatnya minus. 
createHutang terdapat:
id
transaksi_id (mengambil id transaksi sama seperti _saveDetailTransaksi mengambil)
nama (mengambil dari TextFormField Nama penghutang)
sisa_hutang (mengambil nilai minus dari bayar.value<totalHarga.value)
status (langsung mengisi 'belum lunas')
tanggal_mulai (waktu sekarang)